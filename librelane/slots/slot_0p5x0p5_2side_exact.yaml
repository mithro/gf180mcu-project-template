# Exact-pad 2-side configuration -- AUTOGENERATED by
# scripts/generate_exact_3side_configs.py.  DO NOT EDIT BY HAND.
#
# Quarter-size slot_1x1 with the N+E edges as bare cut edges (kept SW corner).
# Every signal/power pad is pinned (via a custom PAD_CFG) to the
# EXACT (x,y) it occupies in a real slot_1x1 build.  2 analog pads
# are relocated (not coordinate-preserved) only to keep the RTL
# analog_PAD port from collapsing to [-1:0].
FP_SIZING: absolute
DIE_AREA: [0, 0, 1966, 2561]
CORE_AREA: [442, 442, 1524, 2119]

VERILOG_DEFINES: ["SLOT_0P5X0P5_2SIDE_EXACT"]

# Partial padring (2-sided): use the partial PDN script.
PDN_CFG: dir::pdn_partial.tcl

# Custom pad config: explicit per-pad placement at exact slot_1x1
# coordinates (bypasses pad_cfg.tcl's even auto-distribution).
PAD_CFG: dir::slots/exact/0p5x0p5_2side_exact_pad_cfg.tcl

PAD_SOUTH: [
    clk_pad,
    rst_n_pad,
    "bidir\\[0\\].pad",
    "bidir\\[1\\].pad",
    "bidir\\[2\\].pad",
    "bidir\\[3\\].pad",
    "analog\\[0\\].pad"
]

PAD_EAST: []

PAD_NORTH: []

PAD_WEST: [
    "inputs\\[0\\].pad",
    "inputs\\[1\\].pad",
    "dvss_pads\\[0\\].pad",
    "dvdd_pads\\[0\\].pad",
    "inputs\\[2\\].pad",
    "inputs\\[3\\].pad",
    "inputs\\[4\\].pad",
    "inputs\\[5\\].pad",
    "analog\\[1\\].pad"
]
